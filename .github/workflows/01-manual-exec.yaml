# CI de nível 01 - Disparo manual através de click

# nome da pipeline
name: 'Execução Manual'

# gatilhos ou triggers
# gatilho manual

on:
  workflow_dispatch:

jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    
    # passos do job
    stepts:
      # clone do projeto
      - uses: action/checkout@v4